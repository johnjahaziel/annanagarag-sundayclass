/// The two Sunday services this app supports. A class can have a
/// different teacher and different students for each service — every
/// teacher and student belongs to exactly one.
class Service {
  static const one = 'Service 1';
  static const two = 'Service 2';

  static const options = [one, two];
}
